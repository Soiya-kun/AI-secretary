{
  "name": "@ai-secretary/infra-cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'infra-cdk lint not configured'",
    "test": "pnpm run build && node --test dist/src/lambda/command-api.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.995.0",
    "@aws-sdk/lib-dynamodb": "3.995.0",
    "aws-cdk-lib": "2.239.0",
    "constructs": "10.5.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.160",
    "@types/node": "22.10.10"
  }
}
